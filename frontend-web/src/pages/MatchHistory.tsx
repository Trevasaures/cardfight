import { PageHeader } from "../components/layout/PageHeader";

export function MatchHistory() {
  return (
    <PageHeader
      eyebrow="Match History"
      title="Logged battles"
      description="Soon this page will replace the Streamlit match history with cleaner cards, filters, and editing."
    />
  );
}